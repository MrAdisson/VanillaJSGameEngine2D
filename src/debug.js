// import { GameManager } from './gameManager.js';

export const logClickedCell = (event, ctx) => {
  // const game = GameManager.getInstance();
  // const grid = game.getGrid();
  // const camera = game.camera;
  // console.log(event.offsetY);
  // console.log(event.clientX);
  // console.log(grid.CELL_SIZE);
  // const clickedCellCoordinates = {
  //   x: Math.floor(camera.x / grid.CELL_SIZE) - Math.floor((ctx.canvas.width / 2 - event.offsetX) / grid.CELL_SIZE),
  //   y: Math.floor((camera.y - (ctx.canvas.height / 2 - event.offsetY)) / grid.CELL_SIZE),
  // };
  // console.log(clickedCellCoordinates);
  // TODO // BROKEN WITH NEW CAMERA SYSTEM + FLEX CANVAS
};
